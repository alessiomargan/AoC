# requirements.txt
# Generated from aoc_env_2024.yml (conda->pip approximation)
# Use `pip install -r requirements.txt` or prefer the conda environment file `aoc_env_2024.yml`.

# Pin versions to avoid slow dependency resolution and ensure wheels are available
numpy>=1.24,<2.0
#jupyter
jupyterlab>=4.0,<5.0
ipykernel>=6.20
more-itertools
bigtree
line_profiler
memory-profiler
pipdeptree
requests
pillow
matplotlib>=3.7,<3.10

# packages originally under pip: (leave as listed)
advent-of-code-data
browser-cookie3
dict-to-dataclass
networkx
pygraphviz

# Dev/test
pytest
