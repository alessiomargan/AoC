{
  "name": "Advent of Code Workspace",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "aoc-jupyter",
  "workspaceFolder": "/workspace",
  "remoteUser": "aoc",

  // Features to install (alternative to Dockerfile if you want modularity)
  "features": {},

  // VS Code settings
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/opt/conda/envs/aoc/bin/python",
        "python.linting.enabled": true,
        "python.linting.pylintEnabled": false,
        "python.formatting.provider": "none",
        "jupyter.notebookFileRoot": "${workspaceFolder}",
        "files.watcherExclude": {
          "**/__pycache__/**": true,
          "**/lp_*": true
        }
      },
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-toolsai.jupyter",
        "ms-toolsai.jupyter-keymap",
        "ms-toolsai.jupyter-renderers",
        "GitHub.copilot",
        "GitHub.copilot-chat"
      ]
    }
  },

  // Forward Jupyter port
  "forwardPorts": [8888],
  "portsAttributes": {
    "8888": {
      "label": "JupyterLab",
      "onAutoForward": "notify"
    }
  },

  // Post-create command: install pre-commit hooks as the non-root user and print a hint
  //"postCreateCommand": "/opt/conda/envs/aoc/bin/pre-commit install && echo 'Advent of Code workspace ready! Open http://localhost:8888 for JupyterLab'",
  "postCreateCommand": "echo 'Advent of Code workspace ready! Open http://localhost:8888 for JupyterLab'",
  // Mount host's .gitconfig for git/GitHub operations
  "mounts": [
    "source=${localEnv:HOME}/.gitconfig,target=/home/aoc/.gitconfig,type=bind,consistency=cached",
    "source=${localEnv:HOME}/.config/aocd,target=/home/aoc/.config/aocd,type=bind,consistency=cached"
  ],

  // Keep container running
  "shutdownAction": "stopCompose",

  // Use the non-root user created in the image (UID/GID set via docker-compose build args)

  // In Codespaces you can’t directly access your local $HOME
  // Store the session token as a Codespaces secret.
  // Inject it via an environment variable AOC_SESSION.
  "containerEnv": {
    "AOC_SESSION": "${localEnv:AOC_SESSION}"
  }
}
